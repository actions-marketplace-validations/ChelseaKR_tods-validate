# Data card: the shipped example feed

**Tier:** L1 (public, non-sensitive)

| Field | Value |
| --- | --- |
| Source | Written by this project, at `examples/sample-feed/`. Hand-authored to be a small, valid TODS + GTFS package a reader can run the tool against in one command. |
| Licence | Apache-2.0, the repository's licence. |
| Fetch/refresh cadence | Not fetched. Refreshed when the spec version it targets changes. |
| Fetch timestamp | Not applicable; versioned with the repository. |
| Known limitations | **Synthetic**, and deliberately tiny. It demonstrates the shape of a feed, not the scale of one; `docs/BENCHMARKS.md` is where scale lives, and its feeds are also synthetic. |
| Retention | Indefinite (L1). |
